import React from "react";

function Header(props) {
    return (
        <header className="d-flex justify-content-between">
            <h1 className="text-primary">GeminiAd</h1>
        </header>
    );
}

export default Header;